let r = 0.999;
let sum = 0.0;
let term = 1.0;
let i = 0;
let start = Date.to_millis();
while (i < 1000000) {
    sum = sum + term;
    term = term * r;
    i = i + 1;
}
let end = Date.to_millis();
print("Sum: " + sum);
print("Time: " + (end - start) + "ms");

