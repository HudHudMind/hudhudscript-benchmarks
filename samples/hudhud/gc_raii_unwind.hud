let caught = 0;
for (let i = 0; i < 50000; i = i + 1) {
    try { let big = [i, i, i]; throw "e"; }
    catch (e) { caught = caught + 1; }
    finally { caught = caught + 0; }
}
print("caught=" + caught);
print("DONE");
