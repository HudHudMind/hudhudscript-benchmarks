fn diff(a, b) { let r=[]; for(let i=0; i<a.length; i=i+1){let f=false; for(let j=0; j<b.length; j=j+1){if(a[i]==b[j]){f=true;}} if(!f){r.push(a[i]);}} return r; }
print(diff([1,2], [2,3]));
print("DONE");
