fn dedup(arr) { let r=[]; for(let i=0; i<arr.length; i=i+1){let f=false; for(let j=0; j<r.length; j=j+1){if(r[j]==arr[i]){f=true;}} if(!f){r.push(arr[i]);}} return r; }
print(dedup([1,2,2,3]));
print("DONE");
