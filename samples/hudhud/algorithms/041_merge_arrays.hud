fn merge_arr(a, b) {
    let res = [];
    for(let i=0; i<a.length; i=i+1) { res.push(a[i]); }
    for(let j=0; j<b.length; j=j+1) { res.push(b[j]); }
    return res;
}
print(merge_arr([1,3], [2,4])[0]);
print("DONE");
