fn is_prime(n) { if(n<=1){return false;} for(let i=2; i*i<=n; i=i+1){if(n%i==0){return false;}} return true; } fn primes(n) { let res=[]; for(let i=2; i<=n; i=i+1){if(is_prime(i)){res.push(i);}} return res; }
print(primes(20));
print("DONE");
