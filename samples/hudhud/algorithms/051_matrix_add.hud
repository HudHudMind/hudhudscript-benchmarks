fn madd(a, b) { let r=[]; for(let i=0; i<a.length; i=i+1){let row=[]; for(let j=0; j<a[i].length; j=j+1){row.push(a[i][j]+b[i][j]);} r.push(row);} return r; }
print(madd([[1]], [[2]]));
print("DONE");
