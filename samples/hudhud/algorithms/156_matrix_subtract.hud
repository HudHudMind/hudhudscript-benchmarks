fn msub(a, b) { let r=[]; for(let i=0; i<a.length; i=i+1){let row=[]; for(let j=0; j<a[i].length; j=j+1){row.push(a[i][j]-b[i][j]);} r.push(row);} return r; }
print(msub([[2]], [[1]]));
print("DONE");
