// 01_simple_done.hud — single step loop with done gate
loop first_loop {
  step first_step {
    result.code = 0

    gate first_gate {
      when result.code == 0 -> done
      else -> fail
    }
  }
}

run loop first_loop;
print("DONE");
