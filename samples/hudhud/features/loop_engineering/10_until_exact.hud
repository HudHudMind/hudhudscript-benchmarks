// 10_until_exact.hud — until(counter >= 3) exact stop
let counter = 0;

loop U mode: until(counter >= 3) {
  step s {
    counter = counter + 1;
    gate G { when true -> continue else -> fail }
  }
}

run loop U;
print("DONE");
