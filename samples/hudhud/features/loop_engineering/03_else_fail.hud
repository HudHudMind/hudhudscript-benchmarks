// 03_else_fail.hud — loop where gate falls through to fail
loop fail_path {
  step only_step {
    result.code = 9

    gate only_gate {
      when result.code == 0 -> done
      else -> fail
    }
  }
}

run loop fail_path;
print("DONE");
