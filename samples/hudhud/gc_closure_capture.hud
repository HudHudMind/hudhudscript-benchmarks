fn make_counter(start) {
    let count = start;
    return () => { count = count + 1; return count; };
}
let total = 0;
for (let i = 0; i < 20000; i = i + 1) {
    let c = make_counter(i);
    total = total + c() + c();
}
print("total=" + total);
print("DONE");
